import { feedbackClasses } from '../../../helpers/feedback-classes';
import { module, test } from 'qunit';

module('Unit | Helper | feedback classes');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = feedbackClasses(42);
  assert.ok(result);
});
